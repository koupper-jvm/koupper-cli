package %PACKAGE%

import com.koupper.octopus.annotations.Export

data class Input(val payload: String?)
data class Output(val message: String?)

@Export
val myScript: (Input) -> Output = { _ ->
    Output("SUCCESS")
}

import com.koupper.octopus.process.ScriptProcessor

val setup: (ScriptProcessor) -> ScriptProcessor = { processor ->
    processor.register(
        "project-name",
        "LOCAL_AWS_DEPLOYMENT",
        "1.0.0",
        "ninja.thedeveloper.registry.service",
        mapOf(
            "registration" to "register-user.kts",
        )
    ).run()

    processor
}